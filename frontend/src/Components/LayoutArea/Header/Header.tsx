import { Typography } from "@mui/material";
import "./Header.css";

function Header(): JSX.Element {
    return (
        <div className="Header">
            <Typography variant="h3">Vacationland</Typography>
        </div>
    );
}

export default Header;
